<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to "✏️ Modifier la tâche", edit_task_path(@task) %> |

<%= link_to "⬅️ Retour à la liste des tâches", root_path %>
